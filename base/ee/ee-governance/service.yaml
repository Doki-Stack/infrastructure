apiVersion: v1
kind: Service
metadata:
  name: ee-governance
  labels:
    app.kubernetes.io/name: ee-governance
    app.kubernetes.io/component: ee
    app.kubernetes.io/part-of: doki-stack
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: ee-governance
