apiVersion: v1
kind: Service
metadata:
  name: ee-notifications
  labels:
    app.kubernetes.io/name: ee-notifications
    app.kubernetes.io/component: ee
    app.kubernetes.io/part-of: doki-stack
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: ee-notifications
