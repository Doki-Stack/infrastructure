apiVersion: apps/v1
kind: Deployment
metadata:
  name: ee-license-server
  labels:
    app.kubernetes.io/name: ee-license-server
    app.kubernetes.io/component: ee
    app.kubernetes.io/part-of: doki-stack
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: ee-license-server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ee-license-server
        app.kubernetes.io/component: ee
        app.kubernetes.io/part-of: doki-stack
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ee-license-server
          image: ee-license-server:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: VAULT_ADDR
              value: http://vault.doki-data.svc.cluster.local:8200
            - name: DATABASE_URL
              value: postgres://app_service:changeme@postgres-postgresql.doki-data.svc.cluster.local:5432/ai_automation
          resources:
            requests:
              memory: 64Mi
              cpu: 50m
            limits:
              memory: 256Mi
              cpu: 250m
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
  strategy:
    type: RollingUpdate
