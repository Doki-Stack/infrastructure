apiVersion: v1
kind: Secret
metadata:
  name: vllm-hf-token
  namespace: doki-ai
  labels:
    app: vllm
    app.kubernetes.io/name: vllm
    app.kubernetes.io/part-of: doki-stack
  annotations:
    doki.io/purpose: "HuggingFace token for vLLM model download — replace with ExternalSecret in production"
type: Opaque
stringData:
  token: "REPLACE_WITH_HF_TOKEN"
