apiVersion: v1
kind: Service
metadata:
  name: mcp-execution
  labels:
    app.kubernetes.io/name: mcp-execution
    app.kubernetes.io/component: mcp
    app.kubernetes.io/part-of: doki-stack
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: mcp-execution
